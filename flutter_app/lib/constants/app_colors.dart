import 'package:flutter/material.dart';

class AppColors {
  // ── Background ─────────────────────────────────────────────────
  static const Color bg          = Color(0xFF121218);
  static const Color bgCard      = Color(0xFF1C1C28);
  static const Color bgElevated  = Color(0xFF252536);
  static const Color bgInput     = Color(0xFF2A2A3C);
  static const Color bgSurface   = Color(0xFF1E1E2C);

  // ── Primary (Teal) ─────────────────────────────────────────────
  static const Color primary     = Color(0xFF26A69A);
  static const Color primaryLight= Color(0xFF4DB6AC);
  static const Color primaryDark = Color(0xFF00897B);

  // ── Accent (Cyan/Blue) ─────────────────────────────────────────
  static const Color accent      = Color(0xFF00BCD4);
  static const Color accentLight = Color(0xFF4DD0E1);

  // ── Text ───────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFF0F0F5);
  static const Color textSecond  = Color(0xFFA0A0B8);
  static const Color textMuted   = Color(0xFF606078);
  static const Color textHint    = Color(0xFF50506A);

  // ── Status ─────────────────────────────────────────────────────
  static const Color success     = Color(0xFF4CAF50);
  static const Color successLight= Color(0xFF66BB6A);
  static const Color warning     = Color(0xFFFFA726);
  static const Color danger      = Color(0xFFEF5350);
  static const Color dangerDark  = Color(0xFFD32F2F);
  static const Color info        = Color(0xFF42A5F5);

  // ── Income / Expense ───────────────────────────────────────────
  static const Color income      = Color(0xFF4CAF50);
  static const Color expense     = Color(0xFFEF5350);

  // ── Glass / Border ─────────────────────────────────────────────
  static const Color glassBorder = Color(0x1AFFFFFF);
  static const Color glassBg     = Color(0x0AFFFFFF);
  static const Color divider     = Color(0xFF2A2A3C);

  // ── Numpad ─────────────────────────────────────────────────────
  static const Color numpadBg    = Color(0xFF1A1A26);
  static const Color numpadKey   = Color(0xFF252536);
  static const Color numpadOp    = Color(0xFF3A3A50);
  static const Color numpadDel   = Color(0xFFD32F2F);
  static const Color numpadEq    = Color(0xFF5C6BC0);
  static const Color numpadOk    = Color(0xFF4CAF50);

  // ── Category Tag Colors ────────────────────────────────────────
  static const Color catFood     = Color(0xFFFF8A65);
  static const Color catTransport= Color(0xFF42A5F5);
  static const Color catShopping = Color(0xFFEC407A);
  static const Color catHealth   = Color(0xFF66BB6A);
  static const Color catEntertain= Color(0xFFAB47BC);
  static const Color catBills    = Color(0xFFEF5350);
  static const Color catEducation= Color(0xFF5C6BC0);
  static const Color catSalary   = Color(0xFF26A69A);
  static const Color catInvest   = Color(0xFF7E57C2);

  // ── Gradients ──────────────────────────────────────────────────
  static const List<Color> gradientPrimary = [Color(0xFF26A69A), Color(0xFF00BCD4)];
  static const List<Color> gradientIncome  = [Color(0xFF4CAF50), Color(0xFF66BB6A)];
  static const List<Color> gradientExpense = [Color(0xFFEF5350), Color(0xFFD32F2F)];
  static const List<Color> gradientGold    = [Color(0xFFFFA726), Color(0xFFFF9800)];
  static const List<Color> gradientBlue    = [Color(0xFF5C6BC0), Color(0xFF42A5F5)];
  static const List<Color> gradientPurple  = [Color(0xFF7E57C2), Color(0xFFAB47BC)];

  // ── Need / Want / Saving ───────────────────────────────────────
  static const Color typeNeed    = Color(0xFF42A5F5);
  static const Color typeWant    = Color(0xFFFFA726);
  static const Color typeSaving  = Color(0xFF66BB6A);
}
